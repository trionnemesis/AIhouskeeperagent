# deps / build
node_modules/
dist/
build/
*.tsbuildinfo

# env / secrets（絕不入版控，見 spec-kit/02-spec/configuration.md）
.env
.env.*
!.env.example

# logs / os
*.log
.DS_Store

# test evidence / reports（實作期產生）
test/evidence/
*.coverage
coverage/

# python
__pycache__/
*.pyc

# venv
.venv/

# 資料 MCP 快取落地（CR-2026-005；ETL 產生，非版控）
var/
*.db

# kustomize render 暫存（conftest/kubeconform 用，非版控）
deploy/_rendered/
.terraform/
*.tfstate*
.terraform.lock.hcl
