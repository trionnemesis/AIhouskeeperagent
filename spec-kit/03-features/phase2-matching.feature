# language: zh-TW
@p2 @core
功能: 結構化配對（Matching · 護城河核心）
  作為 經紀人
  我要 系統用買方需求自動比對在庫物件
  以便 不靠記憶配對、新人也不吃虧

  背景:
    假設 目前 scope 為 tenant="信義加盟A店", seat="seat-A1"
    而且 "林先生" 需求為 信義區、3房、預算1500-2000萬

  場景: SQL WHERE 結構化配對（可解釋、無外送）
    假設 在庫有物件 L1(信義區,3房,1800萬) 與 L2(大安區,2房,3000萬)
    當 為 "林先生" 執行配對
    那麼 候選 應 包含 L1
    而且 不應 包含 L2
    而且 應發出事件 "MatchCandidatesProduced"
    而且 每個候選 應 標明命中條件（區域/房型/預算）

  場景: 新物件建檔觸發配對掃描
    假設 "林先生" 已建檔
    當 一筆符合其需求的新物件被建檔（發出 "ListingProfiled"）
    那麼 系統 應 觸發配對掃描
    而且 產生配對候選

  場景: 買方需求變更觸發重新配對
    假設 "林先生" 已有配對結果
    當 其需求變更（發出 "CustomerRequirementUpdated"）
    那麼 系統 應 重跑配對掃描
    而且 產生更新後的 "MatchCandidatesProduced"（含先前不符的候選）

  場景: 新物件反向配對通知對應客戶
    當 一筆新物件建檔
    那麼 系統 應 執行 match_customers 反向配對
    而且 通知對應經紀人其名下符合的候選客戶

  場景: 配對候選先通知經紀人而非客戶（Inv-2）
    當 配對候選產生
    那麼 系統 應 通知經紀人
    而且 不應 自動通知客戶
    當 經紀人核准後
    那麼 應發出事件 "MatchPresented"
    而且 才允許對客戶溝通

  @guardrail
  場景: 配對不外送資料、不依賴 embedding（MVP）
    當 執行配對
    那麼 配對 應 僅用本地結構化資料的 SQL WHERE
    而且 不應 將客戶資料送往外部 embedding 服務
